language: php

env: 
  - ILLUMINATE_VERSION="~5.1.20"
  - ILLUMINATE_VERSION="5.2.*"

php:
  - 5.5
  - 5.6
  - 7.0
  - hhvm

before_script:
  - travis_retry composer self-update
  - travis_retry composer require illuminate/auth:${ILLUMINATE_VERSION}
  - travis_retry composer require illuminate/container:${ILLUMINATE_VERSION}
  - travis_retry composer require illuminate/contracts:${ILLUMINATE_VERSION}
  - travis_retry composer require illuminate/database:${ILLUMINATE_VERSION}
  - travis_retry composer require --dev illuminate/cache:${ILLUMINATE_VERSION}
  - travis_retry composer require --dev illuminate/console:${ILLUMINATE_VERSION}
  - travis_retry composer update ${COMPOSER_FLAGS} --no-interaction --prefer-source

script:
  - phpunit
  
